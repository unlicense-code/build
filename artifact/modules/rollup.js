/**
 * Designed to build rollup from existing artifacts.
 * Produces ESM Reuseable Rollup Build as also some
 * Major Plugins and exposes that as dirEntrys 
 * to get loaded in the target Environment.
 */ 
